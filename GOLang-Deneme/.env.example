# Kiosk Go Configuration Example
# Copy this file to .env and modify as needed

# ==================== CORE SETTINGS ====================
# Display mode: STANDARD, OFFICE, or BUILDING
QR_MODE=STANDARD

# Room ID (for STANDARD and OFFICE modes)
ROOM_ID=1

# Building ID (for BUILDING mode)
BUILDING_ID=1

# Backend API URL
API_BASE_URL=https://pve.izu.edu.tr/randevu/device

# ==================== MQTT SETTINGS ====================
MQTT_BROKER_IP=pve.izu.edu.tr
MQTT_BROKER_PORT=8883
MQTT_USE_TLS=true
MQTT_INSECURE_SKIP_VERIFY=false

# ==================== OPTIONAL SERVICES ====================
ENABLE_LOCK_MQTT=false
ENABLE_FINGERPRINT=false
ENABLE_DEVICE_MANAGER=false
ENABLE_UPDATE_SERVICE=false

# ==================== BUILDING MODE SETTINGS ====================
BUILDING_MAX_VISIBLE_ROOMS=4
BUILDING_SLIDE_STEP=1

# ==================== UI SETTINGS ====================
# Edge offset/padding around the entire application (in pixels)
# Useful for TVs/monitors with overscan issues
# Supports CSS-like syntax:
#   EDGE_OFFSET=10           -> 10px all sides
#   EDGE_OFFSET=10 20        -> 10px top/bottom, 20px left/right  
#   EDGE_OFFSET=10 20 30 40  -> 10px top, 20px right, 30px bottom, 40px left
EDGE_OFFSET=0

# ==================== SERVICE CONFIGURATION ====================
DESTINATION_DIR=/home/pi/kiosk
BRANCH_NAME=main
SERVICE_QR=kiosk-qr.service
SERVICE_LOCK=kiosk-lock.service
SERVICE_FINGER=kiosk-finger.service
SERVICE_UPDATE=kiosk-update.service
SERVICE_DEVICEMANAGER=kiosk-dm.service
